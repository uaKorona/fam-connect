# 🚀 Інструкції для деплою fam-connect на Render

## 📋 Передумови

1. **GitHub репозиторій** - код повинен бути у GitHub
2. **Render акаунт** - зареєструйтеся на [render.com](https://render.com)
3. **Налаштована збірка** - переконайтеся що `npm run build` працює локально

## 🔧 Підготовка до деплою

### 1. Перевірка конфігурації збірки

```bash
# Переконайтеся що збірка працює
npm run build

# Перевірте що згенеровані файли на місці
ls -la dist/fam/server/
```

### 2. Перевірка package.json scripts

Переконайтеся що в `fam/package.json` є наступні скрипти:

```json
{
  "scripts": {
    "start": "nx serve",
    "build": "nx build",
    "test": "nx test"
  }
}
```

## 🌐 Деплой на Render

### Варіант A: Використання render.yaml (Рекомендується)

1. **Створіть новий Blueprint Service**

   - Перейдіть на [render.com/dashboard](https://render.com/dashboard)
   - Натисніть "New" → "Blueprint"
   - Підключіть ваш GitHub репозиторій
   - Render автоматично знайде `render.yaml` конфігурацію

2. **Налаштування environment variables**

   ```
   NODE_ENV=production
   PORT=4000
   RENDER=true
   ```

3. **Деплой**
   - Натисніть "Apply" для запуску деплою
   - Дочекайтеся завершення збірки (5-10 хвилин)

### Варіант B: Ручне створення Web Service

1. **Створіть новий Web Service**

   - "New" → "Web Service"
   - Підключіть GitHub репозиторій
   - Виберіть гілку `main`

2. **Налаштування збірки**

   ```
   Name: fam-connect
   Environment: Node
   Build Command: npm ci && npm run build
   Start Command: node dist/fam/server/server.mjs
   ```

3. **Environment Variables**

   ```
   NODE_ENV=production
   PORT=4000
   RENDER=true
   ```

4. **Advanced Settings**
   ```
   Health Check Path: /api/room/status
   Auto-Deploy: Yes
   ```

## ✅ Перевірка деплою

### 1. Статус сервісу

- Перевірте що статус "Live" в Render dashboard
- URL буде у форматі: `https://your-service.onrender.com`

### 2. Тестування функціональності

```bash
# Перевірка API
curl https://your-service.onrender.com/api/room/status

# Очікуваний результат:
{
  "usersCount": 0,
  "hasOffer": false,
  "hasAnswer": false,
  "iceCandidatesCount": 0
}
```

### 3. Перевірка keep-alive

- Відкрийте додаток у браузері
- Натисніть "Підключитися" для початку дзвінка
- Перевірте Developer Console на наявність логів:
  ```
  [render] Запуск keep-alive з інтервалом 12 хвилин
  [render] Keep-alive ping успішний
  ```

## 🔧 Налагодження проблем

### Проблеми збірки

```bash
# Локально перевірте збірку
npm ci
npm run build

# Перевірте лог-файли в Render dashboard
```

### Проблеми із засинанням сервера

1. **Перевірте environment variables** - `RENDER=true` повинна бути встановлена
2. **Перевірте логи** - повинні бути повідомлення про keep-alive ping
3. **Health check** - переконайтеся що `/api/room/status` відповідає

### Проблеми з WebRTC

- **HTTPS обов'язковий** для WebRTC - Render автоматично надає SSL
- **Файрвол** - переконайтеся що ICE candidates можуть проходити
- **STUN/TURN серери** - для складних мережевих конфігурацій

## 📊 Моніторинг

### Render Metrics

- CPU/Memory використання
- Response time
- Error rate

### Application Logs

```bash
# Важливі логи для відслідковування:
[HostingService] Ініціалізовано з провайдером: render
[render] Запуск keep-alive з інтервалом 12 хвилин
[render] Keep-alive ping успішний
```

## 💰 Limits безкоштовного плану

- **750 годин/місяць** активності сервера
- **Засинання** через 15 хвилин бездіяльності
- **100GB bandwidth** на місяць
- **Спільний vCPU** та 512MB RAM

**Для вашого використання:** 30 дзвінків по 1 годині = 30 годин/місяць - цілком достатньо!

## 🔄 Автоматичний деплой

Render автоматично деплоїть при push до `main` гілки:

```bash
git add .
git commit -m "Додав hosting keep-alive функціональність"
git push origin main
```

## 📞 Підтримка

Якщо виникають проблеми:

1. Перевірте [Render документацію](https://render.com/docs)
2. Подивіться логи у Render dashboard
3. Перевірте що всі environment variables встановлені правильно
